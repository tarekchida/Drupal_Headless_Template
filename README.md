# Drupal_Headless_Template
A turnkey Docker environment for running Drupal in Headless / API-first mode, powered by (Nginx + PHP-FPM 8.3) and pre-configured with all necessary modules for decoupled architecture (REST, JSON:API, GraphQL, OAuth2…)
